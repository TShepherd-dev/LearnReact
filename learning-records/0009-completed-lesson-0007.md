# Completed lesson 0007: TypeScript (JSX → TSX)

Tim worked through Lesson 0007 (turning the demo-react-app JSX into typed TSX — `PaginationProps` incl. `onPage: (page: number) => void`, a discriminated-union `ShellAction` for the reducer, typed route table) and scored **3/3** on the retrieval quiz from memory via the copy-results widget:

- Correct: TSX adds "checked prop types" to a React component
- Correct: the reducer's union of actions is a "discriminated union"
- Correct: `onPage` is declared as `onPage: (p: number) => void`

## Evidence
Quiz results block pasted with 3/3 correct, same session as delivery. BEFORE taking the quiz, Tim asked for more explicit content on lesson 0007 and 0006 (reducer/slice/deep-dive, C# grounding); those lessons were strengthened in-session, then he completed this.

## Implications
The typed-contract model lands: components declare their props as types, and the compiler enforces call sites. Seven lessons, seven perfect scores. He explicitly asked to proceed to Lesson 0008 ("Go for Lesson 8"). No remediation.

## Status
Lesson 0007 delivered, quiz complete 3/3, learning recorded. Next: Lesson 0008 — Home page with mock data (`0008-home-page-with-mock-data.html`).