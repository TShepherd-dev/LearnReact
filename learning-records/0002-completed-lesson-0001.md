# Completed lesson 0001: Vue SFC → React component model

Tim worked through Lesson 0001 (translating `Home.vue`'s shape into a React function component) and reported the content was solid. This confirms he can absorb the core Vue→React component-model mapping — the `data()`→`useState`, `methods`→functions, `v-if`→`&&`, `:class`→`className`, `@click`→`onClick` translations — sitting comfortably in the zone of proximal development.

## Evidence
Self-reported completion of lesson 0001 in the follow-up session ("I've just done the first lesson... the content is fine").

## Retrieval practice (quiz, post-spacing)
In the session after lesson 0002 was delivered, Tim returned to lesson 0001's retrieval quiz and scored **3/3** (all three component-model mappings correct from memory, via the copy-results widget):
- Correct: `data()` → `useState`
- Correct: `v-if` → `&&` / ternary
- Correct: `@click` → `onClick`

This is storage-strength evidence for the core mapping after a spacing gap — no remediation needed. The quiz-copy flow works end-to-end and produces the exact format to record.

## Implications
Next lessons can assume he holds the component-model mapping and should push into the next genuinely new territory (state & effects replacing lifecycle) rather than re-covering the component skeleton.

## Status
Note: he also flagged the visual styling of the lesson/reference output as needing work — aesthetic polish is a real preference to address (see NOTES.md), independent of correctness.
